/** Metadados públicos das atualizações exibidas no painel da aplicação. */
export interface ApplicationNotification {
  id: string;
  versao: string;
  titulo: string;
  resumo: string;
  publicadaEm: string;
}
